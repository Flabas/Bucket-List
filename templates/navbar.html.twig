<nav class="navbar navbar-expand-lg custom-navbar shadow-lg" role="navigation" aria-label="Navigation principale">
    <div class="container">
        <a class="navbar-brand custom-brand d-flex align-items-center" href="{{ path('app_main') }}">
            <div class="brand-icon-wrapper me-2">
                <img src="{{ asset('images/logo.svg') }}" alt="Logo Bucket-List" width="32" height="32" class="brand-icon">
            </div>
            <span class="brand-text">Bucket-List</span>
        </a>

        <button class="navbar-toggler custom-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mainNavbar" aria-controls="mainNavbar" aria-expanded="false" aria-label="Basculer la navigation">
            <span class="custom-toggler-icon">
                <span></span>
                <span></span>
                <span></span>
            </span>
        </button>

        <div class="collapse navbar-collapse" id="mainNavbar">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link custom-nav-link {{ app.request.attributes.get('_route') == 'app_main' ? 'active' }}" href="{{ path('app_main') }}">
                        <i class="fas fa-home nav-icon"></i>
                        <span>Accueil</span>
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link custom-nav-link {{ app.request.attributes.get('_route') starts with 'app_wish_' ? 'active' }}" href="{{ path('app_wish_list') }}">
                        <i class="fas fa-lightbulb nav-icon"></i>
                        <span>Idées</span>
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link custom-nav-link {{ app.request.attributes.get('_route') == 'app_about' ? 'active' }}" href="{{ path('app_about') }}">
                        <i class="fas fa-info-circle nav-icon"></i>
                        <span>À propos</span>
                    </a>
                </li>
                {% if app.user and 'ROLE_ADMIN' in app.user.getRoles() %}
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle custom-nav-link {{ app.request.attributes.get('_route') starts with 'app_category_' or app.request.attributes.get('_route') starts with 'admin_user_' ? 'active' }}"
                           href="#"
                           id="adminDropdown"
                           role="button"
                           data-bs-toggle="dropdown"
                           aria-expanded="false">
                            <i class="fas fa-shield-alt nav-icon"></i>
                            <span>Administration</span>
                        </a>
                        <ul class="dropdown-menu custom-dropdown" aria-labelledby="adminDropdown">
                            <li>
                                <a class="dropdown-item custom-dropdown-item {{ app.request.attributes.get('_route') starts with 'app_category_' ? 'active' }}"
                                   href="{{ path('app_category_list') }}">
                                    <i class="fas fa-tags dropdown-icon"></i>
                                    <span>Gestion des catégories</span>
                                </a>
                            </li>
                            <li>
                                <a class="dropdown-item custom-dropdown-item {{ app.request.attributes.get('_route') starts with 'admin_user_' ? 'active' }}"
                                   href="{{ path('admin_user_index') }}">
                                    <i class="fas fa-users dropdown-icon"></i>
                                    <span>Gestion des utilisateurs</span>
                                </a>
                            </li>
                            <li><hr class="dropdown-divider custom-divider"></li>
                            <li>
                                <a class="dropdown-item custom-dropdown-item" href="{{ path('app_test_mail') }}">
                                    <i class="fas fa-envelope-open-text dropdown-icon"></i>
                                    <span>Test Email</span>
                                </a>
                            </li>
                            <li>
                                <a class="dropdown-item custom-dropdown-item" href="{{ path('app_debug_list_users') }}">
                                    <i class="fas fa-bug dropdown-icon"></i>
                                    <span>Debug Utilisateurs</span>
                                </a>
                            </li>
                        </ul>
                    </li>
                {% endif %}
            </ul>

            <ul class="navbar-nav ms-auto mb-2 mb-lg-0 user-nav">
                {% if app.user %}
                    <li class="nav-item dropdown">
                        <a class="nav-link custom-nav-link user-dropdown"
                           href="#"
                           id="userDropdown"
                           role="button"
                           data-bs-toggle="dropdown"
                           aria-expanded="false">
                            <div class="user-info d-flex align-items-center">
                                <div class="user-avatar">
                                    <i class="fas fa-user-circle"></i>
                                </div>
                                <span class="user-text">{{ app.user.pseudo }}</span>
                                <i class="fas fa-chevron-down ms-1 dropdown-arrow"></i>
                            </div>
                        </a>
                        <ul class="dropdown-menu custom-dropdown user-dropdown-menu" aria-labelledby="userDropdown">
                            <li>
                                <div class="dropdown-header">
                                    <div class="d-flex align-items-center gap-2">
                                        <div class="user-avatar-small">
                                            <i class="fas fa-user-circle text-primary"></i>
                                        </div>
                                        <div>
                                            <div class="fw-semibold">{{ app.user.pseudo }}</div>
                                            <small class="text-muted">{{ app.user.email }}</small>
                                        </div>
                                    </div>
                                </div>
                            </li>
                            <li><hr class="dropdown-divider custom-divider"></li>
                            <li>
                                <a class="dropdown-item custom-dropdown-item" href="{{ path('app_profile_show') }}">
                                    <i class="fas fa-user dropdown-icon"></i>
                                    <span>Mon profil</span>
                                </a>
                            </li>
                            <li>
                                <a class="dropdown-item custom-dropdown-item" href="{{ path('app_wish_list') }}">
                                    <i class="fas fa-lightbulb dropdown-icon"></i>
                                    <span>Mes idées</span>
                                </a>
                            </li>
                            <li>
                                <a class="dropdown-item custom-dropdown-item" href="#">
                                    <i class="fas fa-cog dropdown-icon"></i>
                                    <span>Paramètres</span>
                                </a>
                            </li>
                            <li><hr class="dropdown-divider custom-divider"></li>
                            <li>
                                <a class="dropdown-item custom-dropdown-item logout-item" href="{{ path('app_logout') }}">
                                    <i class="fas fa-sign-out-alt dropdown-icon"></i>
                                    <span>Déconnexion</span>
                                </a>
                            </li>
                        </ul>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a class="nav-link custom-nav-link {{ app.request.attributes.get('_route') == 'app_register' ? 'active' }}" href="{{ path('app_register') }}">
                            <i class="fas fa-user-plus nav-icon"></i>
                            <span>Inscription</span>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link custom-nav-link btn-login {{ app.request.attributes.get('_route') == 'app_login' ? 'active' }}" href="{{ path('app_login') }}">
                            <i class="fas fa-sign-in-alt nav-icon"></i>
                            <span>Connexion</span>
                        </a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>
