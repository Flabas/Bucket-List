{% extends 'base.html.twig' %}

{% block title %}Accueil - Bucket-List{% endblock %}

{% block body %}
<div class="py-4">
    <div class="p-5 mb-4 bg-body-secondary rounded-3">
        <div class="container py-5">
            <h1 class="display-5 fw-bold mb-3 d-flex align-items-center gap-2">
                <img src="{{ asset('images/logo-512.png') }}" alt="Logo" style="width:48px;height:48px;">
                Bienvenue {% if app.user%} {{app.user.pseudo}} {% endif%} sur Bucket-List
            </h1>
            <p class="col-md-10 col-lg-8 fs-5 text-secondary mb-4">
                Collectez, explorez et réalisez vos meilleures idées de vie. Parcourez les inspirations de la communauté
                et gardez une trace de ce que vous souhaitez accomplir.
            </p>
            <div class="d-flex gap-2">
                <a class="btn btn-primary btn-lg" href="{{ path('app_wish_list') }}">Découvrir les idées</a>
                <a class="btn btn-outline-secondary btn-lg" href="{{ path('app_about') }}">En savoir plus</a>
            </div>
        </div>
    </div>

</div>
{% endblock %}
